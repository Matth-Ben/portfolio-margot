{% macro default(data) %}
    {% import "@components/image.twig" as image %}

    <section class="container container--custom" show-elements>
        {{ image.default(data.image, '', 'full', 'full-mobile', true) }}
    </section>
{% endmacro %}